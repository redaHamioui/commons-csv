machine:
  java:
    version: oraclejdk6
 

dependencies:
  override:
    #- docker info
    - mvn dependency:resolve -f pom.xml

general:
  artifacts:
    # publish do core
    - $CIRCLE_ARTIFACTS

test:
  override:
    # build
    - mvn install -f pom.xml jacoco:report 
  

  post:
    # testes report
    - mkdir -p $CIRCLE_TEST_REPORTS
    
   
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS \;
